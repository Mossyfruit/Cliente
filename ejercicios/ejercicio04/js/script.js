"use strict"

/**
 * El cuarto ejercicio nos pide que activemos las funciones al pasar el raton por encima o salir de un recuadro negro realizado con una etiqueta div y decorado en css.
 * El codigo de js no necesita cambiar en nada con respecto al ejercicio anterior
 */

/**
 * Definimos la variable "nombre"
 */
let nombre = "Usuario";

/**
 * Generamos las funciones saludar() y despedir()
 */

function saludar(){
    alert(`Hola ${nombre}`);
}

function despedir(){
    alert(`Adios ${nombre}`);
}