"use strict"

/**
 * En este tercer ejercicio, vamos a usar la etiqueta "button" de html para provocar las funciones saludar y despedir, que provocaran un alert.
 */

/**
 * Definimos la variable "nombre"
 */
let nombre = "Usuario";

/**
 * Generamos las funciones saludar() y despedir()
 */

function saludar(){
    alert(`Hola ${nombre}`);
}

function despedir(){
    alert(`Adios ${nombre}`);
}